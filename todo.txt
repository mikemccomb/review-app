Entries Show
# Subject displays name rather than number
-- Entries index currently shows Subject ID;
--- Edit json to display different data?
-- Entry show currently shows Subject ID;
--- I should be able to pass the name via params from the "Show this entry" link 
-- Entry NEW will display subject name; Entry EDIT will not  
# Category displays name rather than number
-- Might need to resolve Category Show first
# Rating display different
-- Slider bar in place; needs labels


Categories Index

Categories Show

Subjects Show
# Calculate average rating from values in associated entries

Related Tasks
# Calculate average rating of entries with same subject_id (Subjects Show)
# Pre-load values in Entries form; needs to work for both New and Edit 
# Make purdy