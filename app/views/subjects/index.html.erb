<div class="container">
<p style="color: green"><%= notice %></p>

<h1>All Subjects</h1>
<div>
<p>
<%= link_to "New subject", new_subject_path %> |
<%= link_to "Back to categories", categories_path %>
</p>
</div>

<div id="subjects" class="d-grid gap-2">
  <% @subjects.each do |subject| %>
    <%= render subject %>
  <% end %>
</div>
</div>