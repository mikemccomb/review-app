<p style="color: green"><%= notice %></p>

<% @category.subjects.each do |subject| %>
<%= render subject %>
<%= link_to "View this subject", subject_path(subject)%>
<% end %>

<!-- 
    # Make purdy
 -->

<div>
  <%= link_to "Edit this category", edit_category_path(@category) %> |
  <%= link_to "Add new subject", new_subject_path(params: {category_id: @category.id, category_name: @category.name}) %> |
  <%= link_to "Back to categories", categories_path %>

  <%= button_to "Destroy this category", @category, method: :delete %>
</div>
