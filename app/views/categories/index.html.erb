<div class="container">
  <p style="color: green"><%= notice %></p>

  <h1>All Categories</h1>

  <div>
  <p><%= link_to "New category", new_category_path, class:"btn btn-success", form: {style: 'display:inline-block;'}%> <%= link_to "See all subjects", subjects_path, class:"btn btn-primary", form: {style: 'display:inline-block;'} %></p>
  </div>

  <div id="categories" class="d-grid gap-2">
    <% @categories.each do |category| %>
      <%= render category %>
    <% end %>
  </div>
</div>
